<UserControl x:Class="Terminfindungsapp.UserControls.HomepageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Terminfindungsapp.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#CAC0B3">
        <Grid Background="#002159" Margin="150, 20, 150, 20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="100*" />
            </Grid.RowDefinitions>

            <Label Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Content="YOUR EVENTS" 
            FontWeight="Bold" FontSize="30" Margin="10, 20, 10, 20" Foreground="#FF595A"/>
            
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Background="#5483dc">
                <StackPanel x:Name="staEvents" Margin="10, 5, 5, 5">
                    <StackPanel Grid.Row="1" Margin="10"></StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
